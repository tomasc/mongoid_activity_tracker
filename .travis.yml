language: ruby
cache: bundler
script: 'bundle exec rake'
sudo: false
rvm:
  - 2.2.3
services:
  - mongodb

notifications:
  email:
    recipients:
      - tomas.celizna@gmail.com
    on_failure: change
    on_success: never
